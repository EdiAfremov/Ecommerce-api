const express = require('express');
var { mongoose } = require('./../db/mongoose')
var { Liked } = require('./../models/likedItemsModel')

const router = express.Router();

router.post('/', function (req, res, next) {

});


module.exports = router;